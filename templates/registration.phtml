<!DOCTYPE html>
<html>
<head>
    <title>Registration</title>
    <link rel='stylesheet' type='text/css' href='/css/stylesheets.css'/>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8"/>
</head>
<body>
	<form method="post" name="registraion" action="/public/registration.php?go=/public/index.php">
		<label>
			Name <input type="text" name="name" pattern="\D{1,20}" required value="<?= ($values['name']) ? htmlspecialchars($values['name'], ENT_QUOTES) : '' ?>">
			<span>
				<?= isset($errors['name']) ? $errors['name'] : '' ?>
			</span>
		</label>

		<label>
			Surname <input type="text" name="surname" pattern="\D{1,20}" required value="<?= ($values['surname']) ? htmlspecialchars($values['surname'], ENT_QUOTES) : '' ?>">
			<span>
				<?= isset($errors['surname']) ? $errors['surname'] : '' ?>
			</span>
		</label>

		<label>
			Gender <input type="radio" name="gender" value="Man">M <input type="radio" name="gender" value="Woman">W
			<span>
				<?= isset($errors['gender']) ? $errors['gender'] : '' ?>
			</span>
		</label>

		<label>
			Grup Number <input type="text" name="grupnumber" pattern="[0-9a-zA-Z]{2,5}" required  value="<?= ($values['grupnumber']) ? htmlspecialchars($values['grupnumber'], ENT_QUOTES) : '' ?>">
			<span>
				<?= isset($errors['grupnumber']) ? $errors['grupnumber'] : '' ?>
			</span>
		</label>

		<label>
			Email <input type="email" name="email" pattern=".+@.+\..+" required value="<?= ($values['email']) ? htmlspecialchars($values['email'], ENT_QUOTES) : '' ?>">
			<span>
				<?= isset($errors['email']) ? $errors['email'] : '' ?>
			</span>
		</label>

		<label>
			SAT Scores <input type="number" name="satscores" value="<?= ($values['satscores']) ? htmlspecialchars($values['satscores'], ENT_QUOTES) : '' ?>">
			<span>
				<?= isset($errors['satscores']) ? $errors['satscores'] : '' ?>
			</span>
		</label>

		<label>
			Year Of Birth <input type="text" name="yearofbirth" pattern="\d{4}" required  value="<?= ($values['yearofbirth']) ? htmlspecialchars($values['yearofbirth'], ENT_QUOTES)  : '' ?>">
			<span>
				<?= isset($errors['yearofbirth']) ? $errors['yearofbirth'] : '' ?>
			</span>
		</label>

		<label>
			Location <input type="radio" name="location" value="local">Local <input type="radio" name="location" value="nonresident">Nonresident
			<span>
				<?= isset($errors['location']) ? $errors['location'] : '' ?>
			</span>
		</label>

		<label>
			Password <input type="password" name="password" pattern="(.){6,20}" required >
			<span>
				<?= isset($errors['password']) ? $errors['password'] : '' ?>
			</span>
		</label>

		<label>
			Retry Password <input type="password" name="retrypassword" pattern="(.){6,20}" required >
			<span>
				<?= isset($errors['retrypassword']) ? $errors['retrypassword'] : '' ?>
			</span>
		</label>

		<input type="submit" name="input" value="input">
	</form>
</body>
</html>